# Suppress peer-dependency conflicts caused by eslint-plugin-react-hooks@5.2.0
# declaring peers only up to eslint@^9 while the project uses eslint@^10.
# All packages are compatible at runtime — this is purely a declaration gap.
legacy-peer-deps=true
